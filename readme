
在parse_list_page解析中、有一个逻辑没有处理好、
    移动、联通、电信 和 虚拟类型没有一个统一的url xpath提取路径、
    目前做法是 前三个是一个逻辑为一个抓取程序(fetch_mobile)、虚拟的是单独抓取是另外一个抓取程序(fetch_mobile_xuni)
    可以借用博森的建议：通过正则匹配出所有的url ,正则写好了，但是有问题，可以找博森帮忙看看

